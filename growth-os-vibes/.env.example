# Growth OS — Environment variables (copy to .env.local for local dev)
# For Vercel: set these in Project Settings → Environment Variables

# Required for gap analysis (get key: https://aistudio.google.com/app/apikey)
GEMINI_API_KEY=your_gemini_api_key

# Optional: Supabase for waitlist. If unset, waitlist still returns success but does not persist.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
